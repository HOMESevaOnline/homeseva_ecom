rules_version = '2';

// Allow read and write access to the storage bucket for authenticated users
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}